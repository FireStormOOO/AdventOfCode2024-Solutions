using System.Diagnostics;
using Common;
using DayN;
using static Common.TupleMath<int>;

long Calculate(string input)
{
    return 0;
}

string testInput = "";
Debug.Assert(0 == Calculate(testInput));

Console.WriteLine($"Your message here {Calculate(PuzzleInput.Input)}");

Console.WriteLine($"Done!");